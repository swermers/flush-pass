# /reference

Concept art, exploratory renders, and source media that aren't shipped to the
browser. Kept for art direction continuity — re-prompt against these when
generating new game scenes so everything stays in the same world.

Shipped assets live in `/public/images` and `/public/videos`.
